<%= render "cocktails/navfoot" %>


  <div class="banner">
    <h1>
      <center>
        Welcome to Shake It
        <i class="fa fa-glass"></i>
      </center>
    </h1>


    <div class='enumeration'>
      <%= link_to '# ajouter un cocktail #', new_cocktail_path ,:class => "btn btn-primary btn-mini" %>
      <b>
        <% @cocktails.each do |cocktail| %>
        <p><%= link_to cocktail.name, cocktail_path(cocktail) %></p>
        <% end %>
      </b>
    </div>
  </div>

